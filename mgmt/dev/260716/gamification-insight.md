mini-game can put the user as an agent observer inside a 3d-scene where a script typifying a learning point (or collection thereof) unfolds. just like in CATHERINE	(jp game), it repeats groundhog day style until the user identifies the points to notice - that is, the objective elements that make up the thing to be noticed.

-> flow-on thought:
because it all ultimately breaks down to sense data (and perhaps existing background knowledge), we can have background setting (or perhaps portals to other scenes) that reflect prerequisites to understand the current scene.
	e.g. first thing i though of was if a point to notice is primarily visual, then big eye in the background can give the hint
