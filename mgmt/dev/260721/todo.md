1
remove ctrl+mouseOver selector chain display

2
langUnit bubble subString enter press during cycle select to trigger:

```
a. add that subString to linked subSeg input (append)
b. auto-convert to langUnit
c. auto-cycle target that langUnit
d. focus to its child subSeg
```

3
let CLI worker return a "langUnitGuess" property obj ()for target property only):

```
{
  guess: "",
  def: "",
  reason: ""
}
```
