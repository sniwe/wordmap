all code edits use $ponytail skill

maintain a CONTEXT.md file that lists user and app idiosyncratic vocabulary and terminology, pairing against its effective definition, the purpose of which is to impove alignment between user and the AI agent
use the edit-note functionalityStatus maintenance skill to classify and refresh note lifecycle records before changing edit-notes data

codex prompts made to refine edit note edits will create edit note entries to C:\retry\mgmt\edit-notes\notes.json . these will be marked with a "source" property of value "terminal"

bootstrap command ::gacp = "git add commit push". when sent perform those actions. let comment on commit(s) be concise phrase encapsulating last edit made